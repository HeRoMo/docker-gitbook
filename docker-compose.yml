version: "2.2"
services:
  plantuml:
    image: 'bitjourney/plantuml-service:1.3.3'

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - plantuml

  gitbook:
    build: '.'
    image: gitbook
    volumes:
      - ./gitbook:/gitbook
    ports:
      - 4000:4000
    depends_on:
      - nginx
    command: 'serve'
